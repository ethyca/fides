saas_config:
  fides_key: <instance_fides_key>
  name: Simon Data
  type: simon_data
  description: A sample schema representing the Simondata connector for Fides
  user_guide: https://docs.ethyca.com//user-guides/integrations/saas-integrations/simon-data
  version: 0.1.0

  connector_params:
    - name: domain
      default_value: api.simondata.com
    - name: api_token
      label: API token
      sensitive: True

  client_config:
    protocol: https
    host: <domain>
    authentication:
      strategy: bearer
      configuration:
        token: <api_token>

  test_request:
    method: GET
    path: /v1/privacy/export

  endpoints:
    - name: contact
      requests:
        delete:
          method: POST
          path: /v1/privacy/delete/
          body: |
            {
                "description": "Generated by Fides",
                "email_address": "<email>"
            }
          param_values:
            - name: email
              identity: email
