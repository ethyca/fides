coverage:
  status:
    project:
      default:
        target: 88%
    patch:
      default:
        target: 100%
comment:
  after_n_builds: 3
notify:
  after_n_builds: 3
ignore:
  - "**/migrations"
