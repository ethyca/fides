coverage:
  status:
    project:
      default:
        target: 88%
    patch:
      default:
        target: 100%
  notify:
    after_n_builds: 3
  ignore:
    - "**/migrations"

comment:
  after_n_builds: 3
