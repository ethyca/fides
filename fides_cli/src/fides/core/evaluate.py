def evaluate(url: str, manifests_dir: str, fides_key: str = "") -> None:
    """
    Rate a registry against all of the policies within an organization.
    """

    some_var = "Implement this later."
