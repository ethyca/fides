type: feature
description: |
  Add client_id, client_secret, and redirect_uri fields to JiraTicketSchema so Jira OAuth
  app credentials can be configured via the API instead of requiring environment variables.
  Also marks sensitive fields (access_token, refresh_token, client_secret, api_key) so they
  are masked in API responses.
pr: 7901
