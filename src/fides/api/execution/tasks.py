from fides.api.graph.traversal import Traversal


def load_graph_into_db() -> None:
    """
    Load the traversal graph into the database.
    """
    # traversal: Traversal = Traversal(graph, identity)
