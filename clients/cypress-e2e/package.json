{
  "scripts": {
    "clean": "rm -rf node_modules",
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "format": "prettier --write .",
    "format:ci": "prettier --check ."
  },
  "devDependencies": {
    "cypress": "^12.8.1",
    "prettier": "^3.3.3",
    "typescript": "^5.0.2"
  }
}
