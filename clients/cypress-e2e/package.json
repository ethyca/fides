{
  "scripts": {
    "clean": "rm -rf node_modules",
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "format": "prettier --write .",
    "format:ci": "prettier --check ."
  },
  "devDependencies": {
    "cypress": "^13.3.0",
    "prettier": "^2.8.8",
    "typescript": "^5.0.2"
  }
}
