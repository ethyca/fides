export declare class StringUtil {
    /**
     * Throws a SubstringError if the indexes aren't within the length of the string
     */
    static substring(s: string, start: number, end: number): string;
}
