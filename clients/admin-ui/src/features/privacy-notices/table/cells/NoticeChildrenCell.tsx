import { LimitedPrivacyNoticeResponseSchema } from "~/types/api";

const NoticeChildrenCell = ({
  value,
}: {
  value?: LimitedPrivacyNoticeResponseSchema[];
}) => {
  return <span>notice children</span>;
};

export default NoticeChildrenCell;
