import { ManualFieldResponse } from "~/types/api";

export interface Task {
  id: string;
  name: string;
  description: string;
  fieldType: string;
  requestType: string;
  originalField: ManualFieldResponse;
}

// TODO: Update these to use autogenerated types once they're available from the backend API schema
export interface PrivacyRequestFieldDefinition {
  field_path: string;
  field_type: "string" | "integer" | "boolean" | "array";
  description: string;
  is_convenience_field: boolean;
}

export interface PrivacyRequestFieldsResponse {
  privacy_request: {
    [key: string]:
      | PrivacyRequestFieldDefinition
      | { [key: string]: PrivacyRequestFieldDefinition };
  };
}

export enum FieldSource {
  DATASET = "dataset",
  PRIVACY_REQUEST = "privacy_request",
}
