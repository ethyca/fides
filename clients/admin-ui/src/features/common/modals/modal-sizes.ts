/**
 * Named modal widths, aligned to Ant Design breakpoints.
 * For standard modals, omit width entirely (Ant default: 520px).
 */
export const MODAL_SIZE = {
  md: 576,
  lg: 768,
  xl: 992,
  xxl: 1200,
} as const;
