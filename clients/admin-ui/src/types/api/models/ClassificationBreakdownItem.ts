// This file is auto-generated by @hey-api/openapi-ts

import type { ClassificationVendor } from "./ClassificationVendor";

/**
 * ClassificationBreakdownItem
 *
 * One row in the Website Monitor Report's Data Elements table.
 *
 * Inverse pivot of :class:`VendorBreakdownItem` over the same
 * ``(staged_resource × data_use × vendor_id)`` cube: for each Fides
 * ``data_use`` observed in the monitor's non-MUTED resources, lists
 * the distinct vendors that participate.
 */
export type ClassificationBreakdownItem = {
  /**
   * Data Use
   *
   * Fides data_use fides_key, e.g. ``marketing.advertising.first_party.targeted``.
   */
  data_use: string;
  /**
   * Name
   *
   * Human-readable label from ``ctl_data_uses.name`` (mirrors the fideslang taxonomy plus runtime-added custom uses). Falls back to the raw ``fides_key`` when no row matches — losing the row entirely would silently understate the PDF's data-use footprint.
   */
  name: string;
  /**
   * Vendors
   *
   * Vendors participating in this data_use. Alphabetical by resolved display name; the synthetic ``unknown`` entry, when present, is always last.
   */
  vendors?: Array<ClassificationVendor>;
};
