// This file is auto-generated by @hey-api/openapi-ts

import type { BigQueryMonitorConfig } from "./BigQueryMonitorConfig";
import type { OktaMonitorConfig } from "./OktaMonitorConfig";
import type { WebsiteMonitorConfig } from "./WebsiteMonitorConfig";

/**
 * DiscoveryMonitorsConfig
 *
 * Configuration for discovery monitors.
 *
 * Groups the seed inputs for all supported monitor types.
 * See also: MonitorClassifyParams (for llm_model_override).
 */
export type DiscoveryMonitorsConfig = {
  /**
   * BigQuery monitor configuration
   */
  bigquery?: BigQueryMonitorConfig | null;
  /**
   * Website monitor configuration
   */
  website?: WebsiteMonitorConfig | null;
  /**
   * Okta (Identity Provider) monitor configuration
   */
  okta?: OktaMonitorConfig | null;
  /**
   * Llm Model Override
   *
   * LLM model to use for classification (e.g., bedrock/us.anthropic.claude-haiku-4-5-20251001-v1:0)
   */
  llm_model_override?: string;
  /**
   * Wait For Completion
   *
   * Whether to wait for monitor executions to complete before marking the seed as done. When False (default), monitors are started but the seed completes without waiting for them to finish.
   */
  wait_for_completion?: boolean;
};
