// This file is auto-generated by @hey-api/openapi-ts

import type { ConsentPreferencesConfig } from "./ConsentPreferencesConfig";
import type { DiscoveryMonitorsConfig } from "./DiscoveryMonitorsConfig";
import type { MailgunConfig } from "./MailgunConfig";
import type { PrivacyAssessmentsConfig } from "./PrivacyAssessmentsConfig";
import type { S3Config } from "./S3Config";
import type { SeedTasksConfig } from "./SeedTasksConfig";
import type { SlackChatConfig } from "./SlackChatConfig";

/**
 * SeedRequest
 *
 * Request schema for triggering sample-data seeding.
 *
 * .. warning::
 *
 * This is an internal utility for demo, test, and POC environments.
 * It is **not intended for production use** — it will write synthetic
 * data into the target environment.
 *
 * Extra fields are ignored for backward compatibility: older clients or
 * newer payloads with additional keys (e.g. new monitor types) won't fail validation.
 *
 * When ``secret_profile`` is set, the backend resolves the full seed payload
 * from 1Password before execution.  The 1PW payload serves as the *base*
 * (both tasks and credentials); any fields explicitly provided in the
 * request body override the corresponding 1PW values.  Credential
 * validation is deferred until after the merge.
 */
export type SeedRequest = {
  /**
   * Secret Profile
   *
   * Name of a configured seed profile (e.g. 'demo', 'test'). When set, tasks and credentials are loaded from 1Password and merged with any values provided in this request.
   */
  secret_profile?: string | null;
  /**
   * Which seed tasks to run
   */
  tasks?: SeedTasksConfig;
  /**
   * Mailgun credentials (required if messaging_mailgun)
   */
  mailgun?: MailgunConfig | null;
  /**
   * S3 credentials (required if storage_s3)
   */
  s3?: S3Config | null;
  /**
   * Slack chat provider config (required if chat_slack)
   */
  chat_slack?: SlackChatConfig | null;
  /**
   * Privacy assessments config (optional, has sensible defaults)
   */
  privacy_assessments?: PrivacyAssessmentsConfig | null;
  /**
   * Consent preferences config (optional, has sensible defaults)
   */
  consent_preferences?: ConsentPreferencesConfig | null;
  /**
   * Discovery monitor configs (required if discovery_monitors)
   */
  discovery_monitors?: DiscoveryMonitorsConfig | null;
};
