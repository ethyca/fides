// This file is auto-generated by @hey-api/openapi-ts

/**
 * ClassificationVendor
 *
 * Slim vendor reference on a ``ClassificationBreakdownItem`` row.
 *
 * Mirror of :class:`VendorDataUse` for the inverse pivot — pre-resolved
 * ``(id, name)`` so the PDF report can render the row directly without
 * a second lookup. ``vendor_id`` is the literal ``"unknown"`` for the
 * synthetic unattributed entry (matches the Unknown bucket surfaced
 * by ``/vendors``).
 */
export type ClassificationVendor = {
  /**
   * Vendor Id
   *
   * Compass/system vendor identifier, or the literal ``"unknown"`` for unattributed resources.
   */
  vendor_id: string;
  /**
   * Name
   *
   * Resolved display name (System > Compass > Unknown bucket).
   */
  name: string;
};
