// This file is auto-generated by @hey-api/openapi-ts

import type { VendorBreakdownItem } from "./VendorBreakdownItem";

/**
 * VendorBreakdownResponse
 */
export type VendorBreakdownResponse = {
  /**
   * Vendors
   *
   * Per-vendor breakdown rows. Sorted by ``resource_count`` desc; the synthetic ``unknown`` rollup, when present, is always last.
   */
  vendors?: Array<VendorBreakdownItem>;
  /**
   * Monitor Locations
   *
   * Sorted, deduped union of ``datasource_params.locations`` across every monitor in scope, regardless of whether each monitor produced findings. Drives the cover-page "Location Scanned" annotation — needs to render even when ``vendors`` is empty (monitor configured but no resources scanned yet). For the specific locations attributed to a given vendor, see ``VendorBreakdownItem.locations``.
   */
  monitor_locations?: Array<string>;
};
