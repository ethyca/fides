# This file is used by "drill", a load testing utility

---

concurrency: 20
base: 'http://localhost:8080'
iterations: 400
rampup: 2

plan:
  - name: Health Check
    request:
      url: /health

  - name: Worker Health Check
    request:
      url: /health/workers
